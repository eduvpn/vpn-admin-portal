<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <ul class="mainMenu">
      <li><a href="connections">Connections</a></li>
      <li><a href="configurations">Configurations</a></li>
      <li class="active"><a href="info">Info</a></li>
      <li><a href="log">Log</a></li>
      <li><a href="documentation">Documentation</a></li>
    </ul>

    <h2>Info</h2>
    <p>
        This page shows the VPN server configuration.
    </p>

    <dl>
        <dt>IPv4</dt><dd>{{ info.range|e }}</dd>

        <dt>IPv6</dt><dd>{{ info.range6|e }}</dd>

        <dt>DNS</dt>
        <dd>
            <ul>
            {% for d in info.dns %}
                <li>{{ d|e }}</li>
            {% endfor %}
            </ul>
        </dd>

        <dt>Two-factor Authentication</dt>
        <dd>
            {% if info.tfa %}Enabled{% else %}Disabled{% endif%}
        </dd>
    </dl>

    {% include 'footer.twig' %}
</body>
</html>
