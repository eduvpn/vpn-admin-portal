<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <ul class="mainMenu">
      <li><a href="connections">Connections</a></li>
      <li><a href="configurations">Configurations</a></li>
      <li class="active"><a href="info">Info</a></li>
      <li><a href="log">Log</a></li>
      <li><a href="documentation">Documentation</a></li>
    </ul>

    <h2>Info</h2>
    <p>
        This page lists the IP address configuration and allowed destinations. 
        ICMP and DNS (UDP/53, TCP/53) are always allowed for both IPv4 and IPv6.
    </p>
    <table>
        <thead>
            <tr><th>Pool</th><th>IP</th><th>Destination</th></tr>
        </thead>
        <tbody>
            {% for key, pool in info.pools %}
                <tr>
                    <td><span title="{{ key|e }}">{{ pool.name|e }}</span></td>
                    <td><ul class="ipList"><li>{{ pool.range|e }}</li><li>{{ pool.range6|e }}</li></ul></td>
                    <td>
                        <dl>
                        {% if pool.firewall is defined %}
                            {% if pool.firewall.dst_net is defined %}
                                <dt>Network</dt>
                                <dd>
                                    <ul class="ipList">
                                    {% for n in pool.firewall.dst_net %}
                                        <li>{{ n|e }}</li>
                                    {% endfor %}
                                    </ul>
                                </dd>
                            {% endif %}
                        {% endif %}
                            
                        {% if pool.firewall is defined %}
                            {% if pool.firewall.dst_port is defined %}
                                <dt>Port</dt>
                                <dd>
                                    <ul class="ipList">
                                    {% for n in pool.firewall.dst_port %}
                                        <li>{{ n|e }}</li>
                                    {% endfor %}
                                    </ul>
                                </dd>
                            {% endif %}
                        {% endif %}
                        </dl>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    {% include 'footer.twig' %}
</body>
</html>
