<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    <div id="container">
        {% include 'header.twig' %}

        {% if msg %}
            <p class="msg">{{ msg|e }}</p>
        {% endif %}

        <p>Notes:</p>
        <ul>
            <li>This only displays currently connected clients, you cannot block a client that is not currently connected;</li>
        </ul>

        <h2>Active Connections</h2>

	    {% if not clientInfo %}

            <span>No connected clients.</span>	

	    {% else %}
            {% for key, client in clientInfo %}
            <table>
                <tr><th>Client ID</th><td><code>{{ key|e }}</code></tr></tr>
                <tr><th>Client IP</th><td><code>{{ client.client_ip|e }}</code></td></tr>
                <tr>
                    <th>VPN IPs</th>
                    <td>
                        <ul>
                            {% for ip in client.vpn_ip %}
                            <li><code>{{ ip|e }}</code></li>
                            {% endfor %}
                        </ul>
                    </td>
                </tr>
                <tr>
                    <th>Actions</th>
                    <td>
                        <form class="inline" method="post" action="disconnect">
                            <input type="hidden" name="config_id" value="{{ key|e }}">
                            <button type="submit">Disconnect</button>
                        </form>

                        <form class="inline" method="post" action="revoke">
                            <input type="hidden" name="config_id" value="{{ key|e }}">
                            <button type="submit">Revoke</button>
                        </form>

                        <form class="inline" method="post" action="block">
                            <input type="hidden" name="config_id" value="{{ key|e }}">
                            <button disabled="disabled" type="submit">Block</button>
                        </form>
                    </td>
                </tr>
            </table>
            {% endfor %}
        {% endif %}

    </div> <!-- /container -->
</body>
</html>
