<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <ul class="mainMenu">
      <li><a href="connections">Connections</a></li>
      <li class="active"><a href="configurations">Configurations</a></li>
      <li><a href="log">Log</a></li>
      <li><a href="documentation">Documentation</a></li>
    </ul>

    <h2>Edit</h2>
    <p>
        Modifying <em>{{ configName|e}}</em> of user <a href="configurations?userId={{ userId|e }}">{{ userId|e }}</a>.
    </p>
    <form method="post" action="edit">
        <fieldset>
            {% if config.disable %}
                <input type="checkbox" name="disable" id="disable" checked="checked"> 
            {% else %}
                <input type="checkbox" name="disable" id="disable"> 
            {% endif %}
            <label for="disable">Disable</label>
        </fieldset>

        <fieldset>
            <label for="pool">Pool</label>
            <select id="pool" name="pool">
                {% for key, pool in info.ip.v4.pools %}
                    {% if key == config.pool %}
                        <option value="{{ key|e }}" selected="selected">{{ pool.name|e }} ({{ pool.range|e }})</option>
                    {% else %}
                        <option value="{{ key|e }}">{{ pool.name|e }} ({{ pool.range|e }})</option>
                    {% endif %}
                {% endfor %}
            </select>
        </fieldset>
        <fieldset>
            <input type="hidden" name="for_user" value="{{ for_user|e }}">
            <input type="hidden" name="common_name" value="{{ userId|e }}_{{ configName|e}}">
            <button type="submit" name="save" value="save">Save</button>
            <button type="submit" class="warning" name="save" value="save_disconnect">Save &amp; Disconnect</button>

        </fieldset>
    </form>

    {% include 'footer.twig' %}
</body>
</html>
